# Handoff — 2026-09-05

## What changed
- Added `CLAUDE.md`. `README.md` already had a thorough usage header; left unchanged.
- Fixed a line-count error: CLAUDE.md said `entrypoint.sh` is 139 lines; `wc -l` shows 138.

## What I verified against the actual repo
- Makefile targets `test`, `build`, `run`, `web`, `shell` all exist, matching the "How to Run &
  Test" section.
- Layout section (entrypoint.sh, Dockerfile, scripts/test.sh, tests/, examples/, docs/,
  .github/workflows/) matches the actual directory tree.

## Left for Marc
- None outstanding.
